const API_URL = 'https://pokeapi.co/api/v2';

const POKEMON_API = `${API_URL}/pokemon`;

const POKEMON_TYPE_API = `${API_URL}/type`;

module.exports = {
    API_URL,
    POKEMON_API,
    POKEMON_TYPE_API
}