const findPokemonByName = (pokemonsInfo,pokemonName) => {
    // ! esto podria servir para buscar en la bdd
    // if(!pokemonName) throw Error("The pokemon name wasn't provided");
    // if(!pokemonsInfo) throw Error("Pokemons not provided");
    // const pokemonsFound = pokemonsInfo.filter(poke=>poke.name.includes(pokemonName)).map(poke=>(
    //     {
            
    //     }
    // ))
}

module.exports = findPokemonByName;